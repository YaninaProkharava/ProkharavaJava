How to run the app on Windows:
Execute in Command Prompt:
chcp 65001
java -jar "PATH_TO_JAR"
If you have problems with cyrilik while running the app in intelligIdea, channge settings -> editor -> file encodings to UTF-8
